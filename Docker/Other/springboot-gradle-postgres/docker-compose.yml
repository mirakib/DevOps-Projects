services:

  db:
    image: postgres:16-alpine
    container_name: springboot_postgres_db
    restart: unless-stopped
    env_file: .env
    environment:
      POSTGRES_DB: ${DB_NAME}
      POSTGRES_USER: ${DB_USER}
      POSTGRES_PASSWORD: ${DB_PASSWORD}
    volumes:
      - postgres_data:/var/lib/postgresql/data
    ports:
      - "5435:5432"
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U ${DB_USER} -d ${DB_NAME}"]
      interval: 10s
      timeout: 5s
      retries: 5
      start_period: 15s

  web:
    build: .
    container_name: springboot_gradle_web
    restart: unless-stopped
    env_file: .env
    ports:
      - "8082:8080"
    depends_on:
      db:
        condition: service_healthy

volumes:
  postgres_data:
    name: springboot_gradle_postgres_data
